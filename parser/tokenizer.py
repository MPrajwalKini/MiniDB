"""
MiniDB SQL Tokenizer (Lexer)
============================
Converts raw SQL strings into a stream of typed tokens.
Handles keywords, identifiers, literals, operators, and punctuation.

Status: NOT_STARTED — Skeleton only.
"""

# TODO: Implement in Phase 4
# - Token class (type, value, position)
# - TokenType enum (KEYWORD, IDENTIFIER, INTEGER, FLOAT, STRING, etc.)
# - tokenize(sql: str) -> list[Token]
